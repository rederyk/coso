{
  "namespace": "gpio",
  "description": "Controllo pin GPIO digitali per input/output",
  "functions": {
    "write": {
      "signature": "gpio.write(pin: number, value: boolean)",
      "description": "Scrive valore digitale HIGH/LOW su pin GPIO",
      "parameters": {
        "pin": {
          "type": "number",
          "description": "Numero pin GPIO (es. 23, 25, 26)",
          "required": true
        },
        "value": {
          "type": "boolean",
          "description": "true = HIGH (3.3V), false = LOW (0V)",
          "required": true
        }
      },
      "returns": "void",
      "examples": [
        {
          "code": "gpio.write(23, true)",
          "description": "Accende pin GPIO 23"
        },
        {
          "code": "gpio.write(25, false)",
          "description": "Spegne pin GPIO 25"
        }
      ]
    },
    "read": {
      "signature": "gpio.read(pin: number) -> boolean",
      "description": "Legge valore digitale da pin GPIO configurato come input",
      "parameters": {
        "pin": {
          "type": "number",
          "description": "Numero pin GPIO da leggere",
          "required": true
        }
      },
      "returns": "boolean - true se HIGH, false se LOW",
      "examples": [
        {
          "code": "local value = gpio.read(26)\nif value then println('Pin HIGH') else println('Pin LOW') end",
          "description": "Legge e stampa stato pin 26"
        }
      ]
    },
    "toggle": {
      "signature": "gpio.toggle(pin: number)",
      "description": "Inverte stato corrente del pin GPIO (HIGH -> LOW, LOW -> HIGH)",
      "parameters": {
        "pin": {
          "type": "number",
          "description": "Numero pin GPIO da invertire",
          "required": true
        }
      },
      "returns": "void",
      "examples": [
        {
          "code": "gpio.toggle(23)",
          "description": "Inverte stato pin 23"
        }
      ]
    }
  },
  "notes": [
    "I pin GPIO supportati dipendono dall'hardware (ESP32 tipicamente GPIO 0-39)",
    "Alcuni pin sono riservati (UART, SPI, I2C) - verificare pinout",
    "Tensione output: 3.3V (non 5V tollerante!)",
    "Corrente massima per pin: ~40mA"
  ]
}
